import 'package:flutter/material.dart';

class IbanDetailsWidget extends StatelessWidget {
  const IbanDetailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
